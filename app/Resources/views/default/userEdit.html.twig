{% extends 'default/user.html.twig' %}

{% block body %}
    {{ parent() }}
    <br>
    <div class="Lable" style="color: green"> Please edit your personal details </div>
    <br>
    <div class="Lable">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    </div>
{% endblock %}