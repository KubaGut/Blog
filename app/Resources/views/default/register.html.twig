{% extends 'default/index.html.twig' %}

{% block body %}
    {{ parent() }}
    <br>
    <div> Please register your data</div>
    <br>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}