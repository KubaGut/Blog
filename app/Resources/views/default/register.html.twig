{% extends 'default/index.html.twig' %}

{% block body %}
    {{ parent() }}
    <br>
    <div class="Lable"> Please register your data</div>
    <br>
    <div class="Lable">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    </div>
{% endblock %}